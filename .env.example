# Copy to .env and fill in your values. .env is gitignored.
#
# Calendar identifier of a throwaway calendar used by the manual
# save/remove lifecycle test in tests/smoke.test.ts. Find candidates with:
#     node -e "
#     const {EKEventStore} = require('./lib/EventKit');
#     const {EKEntityType} = require('./lib/EKEntityType');
#     const s = EKEventStore.init();
#     for (const c of s.calendars(EKEntityType.EVENT))
#         console.log(c.allowsContentModifications ? 'RW' : 'RO', c.calendarIdentifier, '—', c.title);
#     "
TEST_CALENDAR_ID=
